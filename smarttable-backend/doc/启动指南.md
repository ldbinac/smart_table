# SmartTable Flask 后端启动指南

## 环境要求

- Python 3.11+
- pip 包管理器
- (可选) PostgreSQL 数据库
- (可选) Redis 缓存

## 快速启动（Windows）

### 1. 安装Python

如果系统未安装Python，请从官网下载安装：
- 下载地址：https://www.python.org/downloads/
- 建议版本：Python 3.11.x
- **重要**：安装时勾选 "Add Python to PATH"

### 2. 创建虚拟环境

```powershell
# 进入项目目录
cd d:\_dev\fs_table\smart-table-spec\smarttable-backend

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
.\venv\Scripts\activate
```

### 3. 安装依赖

```powershell
# 确保在虚拟环境中
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt
```

### 4. 配置环境变量

项目已创建 `.env` 文件，使用SQLite数据库（无需额外安装）：

```env
FLASK_APP=run.py
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=smarttable-secret-key-2024
DATABASE_URL=sqlite:///smarttable.db
JWT_SECRET_KEY=smarttable-jwt-secret-key-2024
```

### 5. 初始化数据库

```powershell
# 创建数据库表
python run.py init-db
```

### 6. 启动服务

```powershell
# 方式1：使用Python直接启动
python run.py

# 方式2：使用Flask命令（需先设置环境变量）
$env:FLASK_APP="run.py"
flask run --host=0.0.0.0 --port=5000
```

## Docker 启动（推荐）

如果已安装Docker，可以使用Docker快速启动：

```powershell
# 1. 构建镜像
docker build -t smarttable-backend .

# 2. 运行容器
docker run -d \
  -p 5000:5000 \
  -e FLASK_ENV=development \
  -e DATABASE_URL=sqlite:///app/smarttable.db \
  -v ${PWD}/uploads:/app/uploads \
  --name smarttable-backend \
  smarttable-backend
```

或使用 Docker Compose：

```powershell
# 启动所有服务（包含PostgreSQL和Redis）
docker-compose up -d

# 执行数据库迁移
docker-compose --profile migrate run --rm migrate
```

## 验证服务

### 1. 检查服务状态

```powershell
# 查看端口监听
netstat -an | findstr 5000

# 或使用PowerShell
Get-NetTCPConnection -LocalPort 5000
```

### 2. 测试API

```powershell
# 测试健康检查
curl http://localhost:5000/api/health

# 预期响应：
# {"status":"healthy","timestamp":"2025-01-10T12:00:00"}
```

### 3. 浏览器访问

- API文档：http://localhost:5000/api/
- 健康检查：http://localhost:5000/api/health

## 常见问题

### 问题1：'python' 不是内部或外部命令

**解决**：
1. 检查Python是否安装：`py --version`
2. 如果安装了Python但未添加到PATH，使用完整路径：`C:\Python311\python.exe`
3. 或使用 `py` 命令：`py run.py`

### 问题2：依赖安装失败

**解决**：
```powershell
# 升级pip
python -m pip install --upgrade pip

# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 问题3：端口被占用

**解决**：
```powershell
# 查找占用5000端口的进程
netstat -ano | findstr 5000

# 结束进程（将<PID>替换为实际进程ID）
taskkill /PID <PID> /F

# 或使用其他端口启动
python run.py --port 5001
```

### 问题4：数据库连接失败

**解决**：
- 如果使用SQLite：确保目录有写入权限
- 如果使用PostgreSQL：检查数据库服务是否启动，连接字符串是否正确

## 开发模式启动

### 热重载模式

```powershell
# 设置环境变量
$env:FLASK_ENV="development"
$env:FLASK_DEBUG="1"

# 启动服务（自动重载）
python run.py
```

### 调试模式

```powershell
# 启用详细日志
$env:LOG_LEVEL="DEBUG"
python run.py
```

## 生产环境部署

### 使用Gunicorn

```powershell
# 安装gunicorn（已在requirements.txt中）
pip install gunicorn

# 启动生产服务器
gunicorn -c gunicorn.conf.py run:app
```

### 使用Waitress（Windows推荐）

```powershell
# 安装waitress
pip install waitress

# 启动服务
waitress-serve --host=0.0.0.0 --port=5000 run:app
```

## 服务管理命令

```powershell
# 创建管理员用户
python run.py create-admin admin@example.com password123 管理员

# 进入交互式Shell
python run.py shell

# 重新初始化数据库（会清空数据）
python run.py init-db
```

## 系统状态检查清单

- [ ] Python 3.11+ 已安装
- [ ] 虚拟环境已创建并激活
- [ ] 所有依赖已安装
- [ ] .env 文件已配置
- [ ] 数据库已初始化
- [ ] 服务成功启动（端口5000）
- [ ] API健康检查通过
- [ ] 前端可以连接到后端

## 联系支持

如遇到无法解决的问题，请检查：
1. 错误日志输出
2. 环境变量配置
3. 端口占用情况
4. 数据库连接状态
