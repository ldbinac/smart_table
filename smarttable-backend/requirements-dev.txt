# ============================================
# SmartTable 开发 / 测试环境依赖
# ============================================
# 本文件继承生产依赖（requirements.txt），仅在此追加 dev / test 专属工具，
# 保证 requirements-dev.txt 是 requirements.txt 的超集（dev ⊇ prod）。
# 使用方式：
#     pip install -r requirements-dev.txt
# ============================================

# 生产运行期依赖（Flask、SQLAlchemy、psycopg3、WeasyPrint、APScheduler 等）
-r requirements.txt

# ============================================
# 测试相关
# ============================================
pytest==7.4.3
pytest-flask==1.3.0
pytest-cov==4.1.0

# ============================================
# 代码质量
# ============================================
flake8==6.1.0
black==23.12.0

# ============================================
# 本地开发常用（非生产必需，仅供本地 dev）
# 注意：celery / minio 未出现在 requirements.txt 中，
#       如需生产环境也使用，请同步加入 requirements.txt。
# ============================================
celery==5.3.6
minio==7.2.0
