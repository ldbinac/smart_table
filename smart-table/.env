# ============================================
# SmartTable 前端环境变量配置
# ============================================
#
# 注意事项：
# - 此文件仅在开发环境（npm run dev）时由 Vite 读取
# - 构建生产版本（npm run build）时，值会被编译进 JS 代码
# - 打包后的程序中，此配置无法动态修改！
#
# 推荐配置：
# - 开发环境：使用 /api（Vite 会自动代理到后端）
# - 打包环境：使用 /api（前后端同源部署）

# API 基础 URL
# 开发模式：/api 或 http://localhost:5000/api
# 打包模式：必须使用 /api（相对路径，自动适配后端端口）
VITE_API_BASE_URL=/api