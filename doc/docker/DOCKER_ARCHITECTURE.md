# SmartTable Docker 部署架构

## 📐 架构设计

### 单镜像统一架构（推荐）

SmartTable 采用单 Docker 镜像包含前后端的统一架构设计，简化部署流程。

```
┌─────────────────────────────────────────────────────┐
│                  Docker Container                    │
│                                                      │
│  ┌──────────────┐    ┌──────────────────────────┐  │
│  │    Nginx     │───▶│  Eventlet WSGI Server    │  │
│  │   (Port 80)  │    │   (Flask App Port 5000)  │  │
│  │              │    │                          │  │
│  │  - 静态文件  │    │  - REST API              │  │
│  │  - 反向代理  │    │  - 业务逻辑/WebSocket     │  │
│  └──────────────┘    └──────────────────────────┘  │
│                                                      │
│  ┌──────────────────────────────────────────────┐  │
│  │           Supervisor (进程管理器)             │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
│  ┌──────────────┐    ┌──────────────────────────┐  │
│  │  SQLite DB   │    │    Uploads Directory     │  │
│  │  (或外部 PG) │    │    (附件存储)             │  │
│  └──────────────┘    └──────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 完整生产架构（微服务）

```
┌─────────────────────────────────────────────────────────────┐
│                     Docker Network                           │
│                                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  PostgreSQL │  │    Redis    │  │    MinIO    │         │
│  │   :5432     │  │   :6379     │  │   :9000     │         │
│  │  (数据库)   │  │   (缓存)    │  │ (对象存储)  │         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                │                 │
│         └────────────────┼────────────────┘                 │
│                          │                                  │
│                   ┌──────▼──────┐                          │
│                   │  SmartTable │                          │
│                   │   :80       │                          │
│                   │  (统一应用)  │                          │
│                   └──────┬──────┘                          │
│                          │                                  │
└───────────────────────────┼──────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │    用户访问    │
                    │  http://host  │
                    └───────────────┘
```

## 🏗️ 镜像结构

### 多阶段构建

```dockerfile
阶段 1: frontend-builder
├── Node.js 20 Alpine
├── 安装前端依赖
├── 构建 Vue.js 应用
└── 输出：/app/frontend/dist

阶段 2: backend-builder
├── Python 3.11 Slim
├── 安装系统依赖
├── 安装 Python 包
└── 输出：/root/.local

阶段 3: production
├── Python 3.11 Slim
├── 安装 Nginx + Supervisor
├── 复制前端构建产物 → /app/static
├── 复制后端代码 → /app
├── 复制 Python 依赖
├── 配置 Nginx
├── 配置 Supervisor
└── 启动：Supervisord
```

## 🔄 请求流程

### 用户请求处理流程

```
用户请求
    │
    ▼
┌─────────────────┐
│   Nginx:80      │ ◀── 接收 HTTP 请求
└────────┬────────┘
         │
         ├─ 静态文件请求 ──▶ /app/static/ (直接返回)
         │
         ├─ /api/* ────────▶ Eventlet WSGI:5000
         │                      │
         │                      ▼
         │                 ┌─────────────┐
         │                 │ Flask App   │
         │                 │  业务处理   │
         │                 └──────┬──────┘
         │                        │
         │                        ▼
         │                 ┌─────────────┐
         │                 │  Database   │
         │                 │   (SQLite/  │
         │                 │ PostgreSQL) │
         │                 └─────────────┘
         │
         └─ /uploads/* ───▶ /app/uploads/ (文件服务)
```

### WebSocket 请求流程（启用实时协作时）

```
用户 WebSocket 请求
    │
    ▼
┌─────────────────┐
│   Nginx:80      │ ◀── 接收 WebSocket 升级请求
└────────┬────────┘
         │
         └─ /socket.io/* ─▶ Eventlet WSGI:5000
                               │
                               ▼
                          ┌─────────────┐
                          │ Flask-SocketIO│
                          │  实时协作处理  │
                          └──────┬──────┘
                                 │
                    ┌────────────┼────────────┐
                    │            │            │
                    ▼            ▼            ▼
              ┌─────────┐ ┌─────────┐ ┌──────────┐
              │  Redis  │ │Database │ │ 其他用户  │
              │ (状态)  │ │(持久化) │ │ (广播)   │
              └─────────┘ └─────────┘ └──────────┘
```

## 📦 数据持久化

### Docker 卷管理

```
宿主机                      Docker 容器
┌─────────────────┐        ┌─────────────────┐
│                 │        │                 │
│  sqlite_data ◀──┼────────┼▶ /app          │  应用数据
│                 │        │                 │
│  uploads_data ◀─┼────────┼▶ /app/uploads  │  上传文件
│                 │        │                 │
│  logs/     ◀───┼────────┼▶ /app/logs     │  日志文件
│                 │        │                 │
└─────────────────┘        └─────────────────┘
```

## 🔐 安全架构

### 分层安全设计

```
┌─────────────────────────────────────┐
│     网络层安全                        │
│  - Docker 网络隔离                   │
│  - 端口映射控制                       │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     应用层安全                        │
│  - Nginx 安全头                      │
│  - HTTPS/TLS (可选)                 │
│  - CORS 策略                         │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     认证层安全                        │
│  - JWT 令牌认证                      │
│  - 密码加密 (bcrypt)                │
│  - Token 黑名单                      │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     数据层安全                        │
│  - SQL 注入防护                      │
│  - 文件上传验证                       │
│  - 敏感信息加密                       │
└─────────────────────────────────────┘
```

## 📊 资源分配建议

### 简单部署（SQLite）

| 组件 | CPU | 内存 | 磁盘 |
|------|-----|------|------|
| SmartTable | 1 核 | 1GB | 2GB |
| **总计** | **1 核** | **1GB** | **2GB** |

### 完整部署（微服务）

| 组件 | CPU | 内存 | 磁盘 |
|------|-----|------|------|
| SmartTable | 2 核 | 2GB | 1GB |
| PostgreSQL | 1 核 | 1GB | 5GB |
| Redis | 0.5 核 | 512MB | 1GB |
| MinIO | 1 核 | 1GB | 10GB |
| **总计** | **4.5 核** | **4.5GB** | **17GB** |

## 🔄 高可用方案

### 水平扩展

```
┌──────────────────────────────────────┐
│         Load Balancer                 │
│         (Nginx/Traefik)              │
└──────────────┬───────────────────────┘
               │
       ┌───────┴───────┐
       │               │
┌──────▼──────┐ ┌──────▼──────┐
│ SmartTable  │ │ SmartTable  │
│  Instance 1 │ │  Instance 2 │
└──────┬──────┘ └──────┬──────┘
       │               │
       └───────┬───────┘
               │
    ┌──────────┴──────────┐
    │                     │
┌───▼────┐         ┌─────▼────┐
│Shared  │         │  Shared  │
│  PG    │         │  Redis   │
└────────┘         └──────────┘
```

## 🎯 性能优化

### 缓存策略

```
用户请求
    │
    ▼
┌─────────────────┐
│   Nginx Cache   │ ◀── 静态资源缓存
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Redis Cache    │ ◀── API 响应缓存
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Flask App     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Database       │
└─────────────────┘
```

### Eventlet WSGI 运行模型

容器中的应用进程由 `docker/server_runner.py` 启动，统一使用 `eventlet.wsgi.server`，
不再依赖 Gunicorn（requirements.txt 中也不存在 gunicorn）：

- eventlet 原生支持 HTTP + WebSocket 混合流量，无需按是否启用实时协作切换 worker 类型；
- `ENABLE_REALTIME=true` 时，SocketIO middleware 自动拦截 `/socket.io/` 完成 WebSocket 升级；
- 并发连接上限由 `eventlet.wsgi.server(max_size=8096)` 控制；
- 进程由 Supervisor 托管，异常退出自动重启。

## 📈 监控指标

### 关键指标

- **应用健康**: `/api/health`
- **响应时间**: Nginx 日志中的 `rt` 字段
- **错误率**: HTTP 5xx 错误占比
- **资源使用**: CPU、内存、磁盘 IO
- **数据库**: 连接数、查询性能
- **缓存**: 命中率、内存使用

### 日志收集

```
┌─────────────┐
│   Nginx     │───▶ /var/log/nginx/access.log
│   Logs      │───▶ /var/log/nginx/error.log
└─────────────┘

┌─────────────┐
│ App Server  │───▶ stdout/stderr
│ (Eventlet)  │───▶ Supervisor 日志
└─────────────┘

┌─────────────┐
│   App       │───▶ /app/logs/smarttable.log
│   Logs      │
└─────────────┘
```

---

**此架构设计支持从单节点到集群的平滑扩展，可根据实际需求选择合适的部署方案。**
