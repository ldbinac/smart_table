# Smart Table 智能数据表格管理系统 — 使用操作手册

> 版本：v1.0  
> 更新日期：2026-04-10  
> 适用对象：所有用户（普通用户、管理员）

---

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 技术架构](#2-技术架构)
- [3. 快速开始](#3-快速开始)
- [4. 功能模块说明](#4-功能模块说明)
  - [4.1 用户认证](#41-用户认证)
  - [4.2 首页与多维表管理](#42-首页与多维表管理)
  - [4.3 数据表操作](#43-数据表操作)
  - [4.4 字段管理](#44-字段管理)
  - [4.5 视图管理](#45-视图管理)
  - [4.6 记录管理](#46-记录管理)
  - [4.7 筛选功能](#47-筛选功能)
  - [4.8 排序功能](#48-排序功能)
  - [4.9 分组功能](#49-分组功能)
  - [4.10 数据导入与导出](#410-数据导入与导出)
  - [4.11 仪表盘](#411-仪表盘)
  - [4.12 分享与协作](#412-分享与协作)
  - [4.12b 实时协作](#412b-实时协作)
  - [4.13 表单功能](#413-表单功能)
  - [4.14 系统设置](#414-系统设置)
  - [4.15 后台管理](#415-后台管理)
- [5. 字段类型详解](#5-字段类型详解)
- [6. 视图类型详解](#6-视图类型详解)
- [7. 模板系统](#7-模板系统)
- [8. 键盘快捷键](#8-键盘快捷键)
- [9. 权限体系说明](#9-权限体系说明)
- [10. 常见问题解答（FAQ）](#10-常见问题解答faq)
- [11. 注意事项](#11-注意事项)

---

## 1. 项目概述

**Smart Table** 是一款功能强大的在线智能数据表格管理系统，类似于 Airtable / 飞书多维表格，旨在让数据管理更简单、更高效。系统支持多种视图模式、丰富的字段类型、数据分组筛选排序、仪表盘可视化、表单收集、协作分享等核心能力，适用于项目管理、客户管理、任务跟踪、库存管理等多种业务场景。

### 核心特性一览

| 特性 | 说明 |
|------|------|
| 多维表管理 | 创建、编辑、删除、收藏多维表格 |
| 20+ 字段类型 | 文本、数字、日期、单选/多选、附件、关联、公式等 |
| 6 种视图模式 | 表格、看板、日历、甘特图、画廊、表单 |
| 数据分组 | 最多 3 级分组，分组内独立编号 |
| 筛选与排序 | 多条件筛选、多字段排序，配置持久化 |
| 数据导入/导出 | 支持 CSV、Excel、JSON 格式 |
| 仪表盘 | 可视化数据展示，支持多种组件 |
| 协作与分享 | 成员管理、链接分享、权限控制 |
| 实时协作 | 多人在线编辑、在线状态、单元格锁定、冲突解决 |
| 表单收集 | 自定义表单、分享收集数据 |
| 模板系统 | 10 种预置模板快速创建 |
| 主题切换 | 浅色/深色/跟随系统 |
| 响应式设计 | 适配桌面端与移动端 |

---

## 2. 技术架构

### 技术栈

| 类别 | 技术 |
|------|------|
| 前端框架 | Vue 3 + TypeScript |
| 状态管理 | Pinia |
| UI 组件库 | Element Plus |
| 路由 | Vue Router (Hash 模式) |
| 本地数据库 | IndexedDB (Dexie.js) |
| 图表 | ECharts (vue-echarts) |
| 表格组件 | VXE Table |
| 构建 | Vite |
| 样式 | SCSS |
| 测试 | Vitest |

### 项目结构

```
smart-table/
├── public/                  # 静态资源
├── src/
│   ├── api/                 # API 客户端与类型定义
│   ├── assets/              # 样式与图片资源
│   ├── components/          # 组件
│   │   ├── auth/            # 登录/注册组件
│   │   ├── base/            # 基础组件（成员列表等）
│   │   ├── common/          # 通用组件（头部、侧边栏、Toast等）
│   │   ├── dashboard/       # 仪表盘组件（KPI、图表、时钟等）
│   │   ├── dialogs/         # 对话框组件（筛选、排序、导入导出等）
│   │   ├── fields/          # 字段组件（20+字段类型）
│   │   ├── filters/         # 筛选组件
│   │   ├── groups/          # 分组组件
│   │   ├── sorts/           # 排序组件
│   │   └── views/           # 视图组件（表格、看板、日历等）
│   ├── composables/         # 组合式函数
│   ├── db/                  # IndexedDB 数据库层
│   │   ├── schema.ts        # 数据库表结构定义
│   │   └── services/        # 数据服务（CRUD操作）
│   ├── layouts/             # 布局组件
│   ├── router/              # 路由配置与守卫
│   ├── services/api/        # 后端 API 服务层
│   ├── stores/              # Pinia 状态管理
│   ├── types/               # TypeScript 类型定义
│   ├── utils/               # 工具函数
│   └── views/               # 页面视图
├── doc/                     # 文档目录
├── package.json
└── vite.config.ts
```

### 数据模型

系统核心数据实体包括：

- **Base（多维表）**：顶层容器，包含多个数据表和仪表盘
- **Table（数据表）**：存储结构化数据的核心单元
- **Field（字段）**：定义数据列的类型与属性
- **Record（记录）**：数据行，以字段ID为键存储值
- **View（视图）**：定义数据的展示方式与筛选排序配置
- **Dashboard（仪表盘）**：可视化数据展示面板
- **Attachment（附件）**：文件附件存储
- **OperationHistory（操作历史）**：记录数据变更

---

## 3. 快速开始

### 环境要求

- Node.js >= 18
- npm >= 9

### 安装与运行

```bash
# 1. 进入项目目录
cd smart-table

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 构建生产版本
npm run build

# 5. 预览构建结果
npm run preview

# 6. 运行测试
npm run test

# 7. 运行测试（监听模式）
npm run test:watch

# 8. 生成测试覆盖率
npm run test:coverage
```

### 后端配置

前端默认连接 `http://localhost:5000/api` 作为后端 API 地址。可通过环境变量 `VITE_API_BASE_URL` 修改：

```bash
# .env.development 文件
VITE_API_BASE_URL=http://your-backend-server/api
```

---

## 4. 功能模块说明

### 4.1 用户认证

#### 注册

1. 访问系统，点击「注册」进入注册页面
2. 填写邮箱地址、用户名和密码
3. 点击「注册」按钮完成注册
4. 注册成功后自动跳转到登录页面

#### 登录

1. 访问系统，在登录页面输入邮箱和密码
2. 点击「登录」按钮
3. 登录成功后跳转到首页
4. 系统使用 JWT Token 进行身份验证，支持 Token 自动刷新

#### 退出登录

1. 点击右上角用户头像
2. 选择「退出登录」
3. 系统将清除本地 Token 并跳转到登录页面

---

### 4.2 首页与多维表管理

首页是用户进入系统后的主界面，提供多维表格的统一管理入口。

#### 首页导航

首页顶部包含四个导航标签：

| 标签 | 功能 |
|------|------|
| 首页 | 展示收藏的多维表和所有多维表卡片 |
| 全部 | 以列表形式管理所有多维表，支持分页 |
| 模板 | 浏览和使用预置模板 |
| 分享 | 查看我分享的和我被分享的多维表 |

#### 创建多维表

1. 点击首页右上角「新建」按钮
2. 在弹出对话框中选择创建方式：
   - **创建空白多维表**：从零开始创建
   - **从模板创建**：选择预置模板快速创建
3. 填写多维表信息：
   - **名称**（必填，1-50字符）
   - **描述**（选填，最多200字符）
   - **图标**：从10个预设图标中选择
   - **颜色**：从8种预设颜色中选择
4. 点击「创建」完成

#### 管理多维表

- **编辑**：点击卡片右下角菜单 → 编辑，修改名称、描述、图标和颜色
- **删除**：点击卡片右下角菜单 → 删除（删除操作不可恢复）
- **收藏/取消收藏**：点击卡片上的星标图标
- **搜索**：在顶部搜索栏输入关键词搜索多维表
- **打开**：点击卡片直接打开多维表（在新标签页中打开）

#### 分享视图

在「分享」标签页中：

- **我分享的**：查看自己创建的分享链接，支持复制链接和删除分享
- **分享给我的**：查看其他用户分享给我的多维表

---

### 4.3 数据表操作

进入多维表后，左侧边栏显示数据表和仪表盘列表。

#### 创建数据表

1. 点击左侧边栏底部的「+ 新建数据表」按钮
2. 在弹出的对话框中输入数据表名称（必填）和描述（选填）
3. 点击「确定」完成创建
4. 新数据表会自动创建一个默认的文本字段和表格视图

#### 管理数据表

- **选择数据表**：在左侧边栏点击数据表名称
- **重命名**：右键菜单或侧边栏菜单 → 编辑
- **复制数据表**：在数据表管理对话框中点击「复制」
- **删除**：右键菜单或侧边栏菜单 → 删除
- **收藏**：点击数据表名称旁的星标图标
- **排序**：拖拽数据表名称前的拖拽手柄调整顺序
- **批量管理**：点击侧边栏底部「管理数据表」打开管理对话框

---

### 4.4 字段管理

字段定义了数据表中每一列的数据类型和属性。

#### 添加字段

1. 在表格视图中，点击工具栏的「字段」按钮
2. 在字段管理对话框中点击「添加字段」
3. 配置字段属性：
   - **字段名称**（必填）
   - **字段类型**：从20+种类型中选择
   - **是否必填**
   - **默认值**
   - **描述**
   - **类型特定选项**（如数字精度、选项列表等）
4. 点击「确定」完成创建

#### 管理字段

- **编辑字段**：在字段管理对话框中点击字段旁的编辑按钮
- **删除字段**：在字段管理对话框中点击字段旁的删除按钮（删除后数据不可恢复）
- **排序字段**：拖拽字段名称前的手柄调整顺序
- **隐藏/显示字段**：切换字段的可见性（视图级别）
- **冻结列**：右键列头 → 冻结列

---

### 4.5 视图管理

视图定义了数据的展示方式，每个数据表可以拥有多个不同类型的视图。

#### 创建视图

1. 点击表格上方的视图切换器中的「+」按钮
2. 选择视图类型（表格/看板/日历/甘特图/画廊/表单）
3. 输入视图名称
4. 点击「确定」完成创建

#### 管理视图

- **切换视图**：点击视图切换器中的视图标签
- **重命名**：右键视图标签 → 重命名
- **复制视图**：右键视图标签 → 复制（复制当前视图的所有配置）
- **删除视图**：右键视图标签 → 删除
- **设为默认**：右键视图标签 → 设为默认视图

#### 视图配置持久化

每个视图独立保存以下配置：

- 筛选条件
- 排序规则
- 分组设置
- 隐藏字段
- 冻结字段
- 行高设置

---

### 4.6 记录管理

记录是数据表中的数据行。

#### 添加记录

- **方式一**：点击表格底部的「+」按钮，在表格行内直接编辑
- **方式二**：点击工具栏或表格底部的「添加记录」按钮，在右侧抽屉中填写
- **方式三**：在看板/日历视图中，点击对应位置的添加按钮
- **方式四**：在分组视图中，点击分组内的添加按钮（自动填充分组字段值）

#### 编辑记录

- **行内编辑**：在表格视图中直接点击单元格进行编辑
- **抽屉编辑**：点击行展开按钮，在右侧抽屉中编辑所有字段

#### 删除记录

- **单条删除**：右键记录行 → 删除，或在记录详情抽屉中点击删除
- **批量删除**：勾选多条记录 → 右键 → 删除选中记录

#### 复制记录

- 右键记录行 → 复制记录，系统会创建一条与原记录值相同的新记录

#### 查看记录历史

- 在记录详情抽屉中，可查看该记录的修改历史

---

### 4.7 筛选功能

筛选功能允许根据条件过滤显示的记录。

#### 使用筛选

1. 点击工具栏的「筛选」按钮
2. 在筛选对话框中添加筛选条件：
   - 选择筛选字段
   - 选择操作符
   - 输入筛选值
3. 可添加多个筛选条件
4. 设置条件组合方式：**AND（全部满足）** 或 **OR（任一满足）**
5. 点击「应用」生效

#### 支持的筛选操作符

| 操作符 | 说明 | 适用字段类型 |
|--------|------|------------|
| 等于 | 值完全匹配 | 所有类型 |
| 不等于 | 值不匹配 | 所有类型 |
| 包含 | 值包含指定文本 | 文本、URL、邮箱、电话 |
| 不包含 | 值不包含指定文本 | 文本、URL、邮箱、电话 |
| 开头是 | 值以指定文本开头 | 文本 |
| 结尾是 | 值以指定文本结尾 | 文本 |
| 为空 | 值为空 | 所有类型 |
| 不为空 | 值不为空 | 所有类型 |
| 大于 | 数值大于指定值 | 数字、日期 |
| 小于 | 数值小于指定值 | 数字、日期 |
| 大于等于 | 数值大于或等于指定值 | 数字、日期 |
| 小于等于 | 数值小于或等于指定值 | 数字、日期 |
| 在范围内 | 日期在指定范围内 | 日期 |
| 早于 | 日期早于指定值 | 日期 |
| 晚于 | 日期晚于指定值 | 日期 |
| 是以下之一 | 值在指定选项中 | 单选、多选 |
| 不是以下之一 | 值不在指定选项中 | 单选、多选 |

#### 清除筛选

- 在筛选对话框中点击「清除」按钮
- 筛选条件会随视图配置持久化保存

---

### 4.8 排序功能

排序功能允许按指定字段和方向对记录排序。

#### 使用排序

1. 点击工具栏的「排序」按钮
2. 在排序对话框中添加排序条件：
   - 选择排序字段
   - 选择排序方向：升序（A→Z）或降序（Z→A）
3. 可添加多个排序条件（按优先级排列）
4. 支持拖拽调整排序条件优先级
5. 点击「应用」生效

#### 清除排序

- 在排序对话框中点击「清除」按钮
- 排序配置会随视图配置持久化保存

---

### 4.9 分组功能

分组功能允许按字段值对记录进行分组展示。

#### 使用分组

1. 点击工具栏的「分组」按钮
2. 在分组对话框中选择分组字段
3. 支持最多 **3 级分组**
4. 点击「应用」生效

#### 分组视图特性

- 每个分组可独立展开/折叠
- 分组内记录独立编号
- 可在分组内直接添加记录（自动填充分组字段值）
- 表头固定（滚动时保持可见）
- 列宽可调整
- 支持斑马纹样式
- 支持行选择
- 支持右键菜单操作（排序、冻结、隐藏、编辑字段）

#### 清除分组

- 在分组对话框中点击「清除」按钮
- 分组配置会随视图配置持久化保存

---

### 4.10 数据导入与导出

#### 导入数据

1. 点击工具栏的「导入」按钮
2. 在导入对话框中选择文件（支持 CSV、Excel、JSON 格式）
3. 预览导入数据
4. 配置字段映射（将文件列映射到数据表字段）
5. 设置是否跳过首行（表头行）
6. 点击「开始导入」
7. 导入完成后自动刷新数据

#### 导出数据

1. 点击工具栏的「导出」按钮
2. 在导出对话框中选择：
   - **导出格式**：CSV、Excel、JSON
   - **导出范围**：全部记录或选中记录
   - **导出字段**：选择需要导出的字段
3. 点击「导出」按钮下载文件

---

### 4.11 仪表盘

仪表盘提供数据可视化展示功能。

#### 创建仪表盘

1. 在左侧边栏点击「+ 新建仪表盘」按钮
2. 输入仪表盘名称和描述
3. 点击「确定」完成创建

#### 仪表盘组件

| 组件 | 说明 |
|------|------|
| KPI 指标卡 | 展示关键指标数值 |
| 实时图表 | 动态数据图表展示 |
| 时钟 | 显示当前时间 |
| 日期 | 显示当前日期 |
| 跑马灯 | 滚动展示文本信息 |

#### 仪表盘操作

- **编辑**：右键侧边栏仪表盘名称 → 编辑
- **删除**：右键侧边栏仪表盘名称 → 删除
- **收藏**：点击仪表盘名称旁的星标
- **排序**：拖拽调整仪表盘顺序
- **布局**：支持网格布局和自由布局
- **自动刷新**：可配置自动刷新间隔

#### 仪表盘分享

- 支持生成分享链接
- 可设置访问密码
- 可设置过期时间
- 可设置最大访问次数

#### 仪表盘模板

- 支持从模板创建仪表盘
- 可将现有仪表盘保存为模板

---

### 4.12 分享与协作

#### 成员管理

1. 在多维表页面，点击顶部导航栏的「成员」按钮
2. 在成员管理对话框中：
   - **查看成员列表**：显示所有成员及其角色
   - **添加成员**：输入邮箱地址，选择角色，发送邀请
   - **批量添加**：支持批量输入多个成员
   - **修改角色**：修改成员的权限角色
   - **移除成员**：将成员从多维表中移除

#### 多维表分享

1. 在多维表页面，点击顶部导航栏的「分享」按钮
2. 在分享对话框中：
   - **创建分享链接**：选择权限（仅查看/可编辑）
   - **设置过期时间**（可选）
   - **复制链接**：一键复制分享链接
   - **管理分享**：查看所有分享链接，可删除或修改

#### 权限角色

| 角色 | 权限 |
|------|------|
| 所有者 (Owner) | 全部权限，包括删除多维表、管理成员 |
| 管理员 (Admin) | 编辑数据、管理字段和视图、管理成员 |
| 编辑者 (Editor) | 编辑数据、管理字段和视图 |
| 评论者 (Commenter) | 查看数据、添加评论 |
| 查看者 (Viewer) | 仅查看数据 |

---

### 4.12b 实时协作

实时协作功能允许多个用户同时在线编辑同一多维表格，系统会实时同步数据变更。

> **注意**：实时协作功能需要后端管理员启用（通过 `--enable-realtime` 启动参数或 `ENABLE_REALTIME=true` 环境变量）。如果后端未启用此功能，协作相关界面将自动隐藏，不影响其他功能使用。

#### 在线状态

当实时协作启用时，您可以看到当前正在编辑同一表格的其他用户：

1. **用户头像**：页面右上角显示当前在线的用户头像列表
2. **光标位置**：其他用户选中的单元格会显示该用户的头像标识
3. **视图位置**：其他用户切换视图时，您可以看到其当前所在的视图

#### 单元格锁定

为防止编辑冲突，系统采用单元格锁定机制：

1. 当用户开始编辑某个单元格时，该单元格自动锁定
2. 其他用户看到该单元格标记为"正在编辑"状态，显示编辑者信息
3. 编辑完成后，锁定自动释放
4. 如果用户意外断开连接，锁定会在超时后自动释放

#### 冲突解决

当两个用户同时编辑同一条记录时，系统使用乐观锁机制检测冲突：

1. **正常保存**：如果记录未被其他人修改，保存成功
2. **冲突提示**：如果记录已被其他人修改，系统弹出冲突解决对话框
3. **解决方式**：
   - **覆盖**：使用您的修改覆盖其他人的修改
   - **刷新**：放弃您的修改，加载最新数据重新编辑

#### 连接状态

页面底部显示实时协作连接状态：

| 状态 | 图标 | 说明 |
|------|------|------|
| 已连接 | 🟢 | 实时协作正常工作 |
| 重连中 | 🟡 | 网络中断，正在尝试重新连接 |
| 已断开 | 🔴 | 连接已断开，操作将缓存到离线队列 |

#### 离线操作

当网络中断时：

1. 您的编辑操作会自动缓存到离线队列（最多100条）
2. 网络恢复后，缓存的操作会自动重放
3. 如果重放时发生冲突，会弹出冲突解决对话框

#### 实时协作功能一览

| 功能 | 说明 |
|------|------|
| 在线用户显示 | 显示当前正在编辑同一表格的用户 |
| 单元格锁定 | 编辑中的单元格自动锁定 |
| 视图同步 | 实时同步其他用户的视图切换 |
| 数据变更推送 | 记录/字段/视图/表格变更实时推送 |
| 冲突检测 | 乐观锁机制检测编辑冲突 |
| 离线队列 | 断线时操作自动缓存 |
| 优雅降级 | 协作不可用时自动降级为普通模式 |

---

### 4.13 表单功能

表单视图允许将数据表转换为可分享的数据收集表单。

#### 创建表单视图

1. 在视图切换器中创建「表单」类型的视图
2. 系统自动生成表单配置

#### 配置表单

1. 在表单视图中点击「配置」按钮
2. 设置表单属性：
   - **表单标题**：显示在表单顶部
   - **表单描述**：表单说明文字
   - **提交按钮文字**：自定义提交按钮文本
   - **提交成功提示**：提交后显示的成功消息
   - **允许重复提交**：是否允许同一用户多次提交
   - **可见字段**：选择表单中显示的字段及顺序

#### 分享表单

1. 在表单视图中点击「分享」按钮
2. 生成表单分享链接
3. 复制链接发送给需要填写表单的用户
4. 用户通过链接访问表单页面填写数据
5. 提交的数据自动写入数据表

---

### 4.14 系统设置

点击顶部导航栏的设置图标进入系统设置页面。

#### 外观设置

| 设置项 | 选项 | 说明 |
|--------|------|------|
| 主题模式 | 浅色/深色/跟随系统 | 切换界面主题 |
| 界面语言 | 简体中文/English | 切换显示语言 |

#### 表格设置

| 设置项 | 选项 | 说明 |
|--------|------|------|
| 行高 | 短/中/高 | 设置表格行高 |
| 显示网格线 | 开/关 | 是否显示表格网格线 |
| 斑马纹 | 开/关 | 是否显示隔行变色 |
| 显示记录数 | 开/关 | 是否显示记录计数 |

#### 数据设置

| 设置项 | 选项 | 说明 |
|--------|------|------|
| 自动保存 | 开/关 | 是否自动保存修改 |
| 自动保存间隔 | 1-30秒 | 自动保存的时间间隔 |
| 删除前确认 | 开/关 | 删除操作前是否弹出确认 |

#### 格式设置

| 设置项 | 选项 | 说明 |
|--------|------|------|
| 日期格式 | YYYY-MM-DD 等 | 日期显示格式 |
| 时间格式 | HH:mm:ss 等 | 时间显示格式 |
| 数字格式 | #,##0.00 等 | 数字显示格式 |
| 货币符号 | ¥ / $ 等 | 货币符号 |

#### 重置设置

- 点击「恢复默认设置」按钮可将所有设置恢复为默认值

---

### 4.15 后台管理

管理员可通过后台管理功能管理用户和系统配置。仅 `admin` 和 `workspace_admin` 角色可访问。

#### 用户管理

1. 访问 `/admin/users` 页面
2. 功能：
   - **查看用户列表**：显示所有用户信息
   - **搜索用户**：按邮箱或姓名搜索
   - **角色筛选**：按角色过滤用户
   - **状态筛选**：按状态（活跃/未激活/已暂停/已删除）过滤
   - **添加用户**：手动创建新用户
   - **编辑用户**：修改用户信息和角色
   - **重置密码**：为用户重置密码
   - **批量删除**：批量删除选中用户

#### 系统配置

1. 访问 `/admin/settings` 页面
2. 管理系统级配置参数

#### 操作日志

1. 访问 `/admin/logs` 页面
2. 查看系统操作日志，包括：
   - 操作时间
   - 操作用户
   - 操作类型（创建/更新/删除）
   - 操作对象（Base/表/字段/记录/视图）
   - 变更详情

---

## 5. 字段类型详解

系统支持以下 20+ 种字段类型：

### 基础字段

| 字段类型 | 图标 | 说明 | 特有配置 |
|----------|------|------|----------|
| 文本 (Text) | 📝 | 单行或多行文本 | 富文本模式、最大长度 |
| 数字 (Number) | 🔢 | 数值类型 | 精度、格式（数字/货币/百分比）、货币符号、前缀、后缀 |
| 日期 (Date) | 📅 | 日期或日期时间 | 包含时间、日期格式 |
| 复选框 (Checkbox) | ✅ | 是/否布尔值 | — |
| 评分 (Rating) | ⭐ | 星级评分 | 最大评分值 |
| 进度 (Progress) | 📊 | 进度百分比 | 显示百分比 |

### 选择字段

| 字段类型 | 图标 | 说明 | 特有配置 |
|----------|------|------|----------|
| 单选 (Single Select) | ☑️ | 从预定义选项中选择一个 | 选项列表（名称+颜色）、允许新增选项 |
| 多选 (Multi Select) | ☑️ | 从预定义选项中选择多个 | 选项列表（名称+颜色）、允许新增选项 |

### 联系方式字段

| 字段类型 | 图标 | 说明 |
|----------|------|------|
| 电话 (Phone) | 📞 | 电话号码 |
| 邮箱 (Email) | 📧 | 电子邮件地址 |
| 链接 (URL) | 🔗 | 网址链接 |

### 高级字段

| 字段类型 | 图标 | 说明 | 特有配置 |
|----------|------|------|----------|
| 附件 (Attachment) | 🐝 | 文件附件 | 接受类型、文件大小限制、数量限制、缩略图设置 |
| 成员 (Member) | 👤 | 用户选择 | — |
| 关联 (Link) | 🔗 | 关联其他数据表的记录 | 目标表、关联关系类型、是否双向关联、是否允许多选 |
| 查找 (Lookup) | 🔍 | 从关联记录中查找值 | 目标字段、聚合类型 |
| 公式 (Formula) | 🔣 | 计算公式 | 公式表达式 |

### 系统字段

| 字段类型 | 图标 | 说明 |
|----------|------|------|
| 自动编号 (Auto Number) | 🔢 | 自动递增编号 |
| 创建人 (Created By) | 👤 | 记录创建者（自动填充） |
| 创建时间 (Created Time) | 🕐 | 记录创建时间（自动填充） |
| 修改人 (Updated By) | 👤 | 记录最后修改者（自动填充） |
| 修改时间 (Updated Time) | 🕐 | 记录最后修改时间（自动填充） |

### 关联字段详解

关联字段支持以下关系类型：

| 关系类型 | 说明 |
|----------|------|
| 一对一 (One to One) | 一条记录关联另一表中的一条记录 |
| 一对多 (One to Many) | 一条记录关联另一表中的多条记录 |
| 多对一 (Many to One) | 多条记录关联另一表中的一条记录 |
| 多对多 (Many to Many) | 多条记录关联另一表中的多条记录 |

### 查找字段聚合类型

| 聚合类型 | 说明 |
|----------|------|
| 单个值 (Single) | 返回单个值 |
| 连接 (Concat) | 将多个值连接为字符串 |
| 求和 (Sum) | 数值求和 |
| 平均值 (Avg) | 数值取平均 |
| 最小值 (Min) | 取最小值 |
| 最大值 (Max) | 取最大值 |
| 计数 (Count) | 计算记录数 |

---

## 6. 视图类型详解

### 表格视图 (Table View)

最基础的数据展示方式，以行列形式展示所有数据。

**特性：**
- 支持行内编辑
- 列宽可拖拽调整
- 列头右键菜单（排序、冻结、隐藏、编辑字段）
- 支持分组展示
- 行选择（单选/多选）
- 虚拟滚动（大数据量优化）
- 斑马纹和网格线

### 看板视图 (Kanban View)

以卡片形式按分组字段展示数据，适用于任务管理、状态跟踪等场景。

**配置项：**
- 分组字段：选择用于分组的单选字段
- 卡片字段：选择卡片上显示的字段
- 封面字段：选择作为卡片封面的附件字段

**操作：**
- 拖拽卡片在分组间移动
- 在分组内添加新记录
- 点击卡片编辑记录

### 日历视图 (Calendar View)

以日历形式展示含日期字段的记录。

**配置项：**
- 日期字段：选择开始日期字段
- 结束日期字段：选择结束日期字段（可选，用于跨天事件）
- 默认视图：日/周/月

**操作：**
- 点击日期添加记录
- 点击事件编辑记录
- 切换日/周/月视图

### 甘特图视图 (Gantt View)

以时间线形式展示项目进度，适用于项目管理场景。

**配置项：**
- 开始日期字段
- 结束日期字段
- 持续时间字段（可选）
- 进度字段（可选）

**操作：**
- 拖拽调整任务时间范围
- 点击任务条编辑记录
- 缩放时间轴

### 画廊视图 (Gallery View)

以大卡片形式展示记录，适用于图片展示、产品目录等场景。

**配置项：**
- 图片字段：选择作为卡面图片的附件字段
- 标题字段：选择作为卡片标题的字段

**操作：**
- 点击卡片编辑记录
- 删除记录

### 表单视图 (Form View)

将数据表转换为可填写的表单，用于数据收集。

详见 [4.13 表单功能](#413-表单功能)。

---

## 7. 模板系统

系统提供 10 种预置模板，帮助用户快速创建多维表格：

| 模板名称 | 图标 | 分类 | 包含数据表 |
|----------|------|------|-----------|
| 项目管理 | 📊 | 项目管理 | 项目、任务 |
| 任务跟踪 | ✅ | 任务管理 | 任务 |
| 客户管理 | 👥 | 客户关系 | 客户 |
| 产品需求 | 💡 | 产品管理 | 需求 |
| 内容日历 | 📅 | 内容运营 | 内容 |
| 库存管理 | 📦 | 库存管理 | 商品 |
| 考勤记录 | ⏰ | 人事管理 | 考勤 |
| 预算管理 | 💰 | 财务管理 | 收支记录 |
| 调查反馈 | 📝 | 用户研究 | 反馈记录 |
| 通讯录 | 📇 | 个人管理 | 联系人 |

#### 使用模板

1. 在首页点击「模板」标签
2. 浏览或搜索模板
3. 点击「预览」查看模板详情（包含的数据表、字段、示例数据）
4. 点击「使用模板」创建多维表
5. 系统将自动创建包含所有数据表、字段、视图和示例数据的多维表

---

## 8. 键盘快捷键

系统支持全局键盘快捷键，可在输入框外的任何页面使用。

### 查看快捷键

- 点击顶部导航栏的快捷键图标，打开快捷键帮助对话框

### 通用快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl/Cmd + N | 新建记录 |
| Ctrl/Cmd + S | 保存当前修改 |
| Ctrl/Cmd + Z | 撤销 |
| Ctrl/Cmd + Shift + Z | 重做 |
| Ctrl/Cmd + F | 打开搜索 |
| Ctrl/Cmd + / | 打开快捷键帮助 |
| Esc | 关闭当前对话框/抽屉 |

### 导航快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl/Cmd + ← | 切换到上一个数据表 |
| Ctrl/Cmd + → | 切换到下一个数据表 |

> 注意：在输入框内编辑时，快捷键不会触发。

---

## 9. 权限体系说明

### 用户角色

系统定义了以下用户角色（全局级别）：

| 角色 | 说明 |
|------|------|
| admin | 系统管理员，拥有所有权限 |
| workspace_admin | 工作区管理员，可管理用户 |
| editor | 编辑者，可编辑数据 |
| viewer | 查看者，仅可查看数据 |

### 多维表成员角色

在多维表级别，定义了以下成员角色：

| 角色 | 编辑数据 | 管理字段/视图 | 管理成员 | 删除多维表 |
|------|---------|-------------|---------|-----------|
| Owner | ✅ | ✅ | ✅ | ✅ |
| Admin | ✅ | ✅ | ✅ | ❌ |
| Editor | ✅ | ✅ | ❌ | ❌ |
| Commenter | ❌ | ❌ | ❌ | ❌ |
| Viewer | ❌ | ❌ | ❌ | ❌ |

### 分享权限

| 权限 | 说明 |
|------|------|
| 仅查看 (view) | 通过分享链接只能查看数据 |
| 可编辑 (edit) | 通过分享链接可以编辑数据 |

### 权限判断逻辑

1. 系统首先检查当前用户是否为多维表所有者（Owner）
2. 然后检查成员列表中的角色
3. 如果通过分享链接访问，根据分享权限确定操作范围
4. 工具栏按钮和操作入口会根据权限自动显示/隐藏

---

## 10. 常见问题解答（FAQ）

### Q1: 如何恢复误删的数据？

**A:** 目前删除操作不可恢复。建议在删除前开启「系统设置 → 删除前确认」选项，系统会在每次删除前弹出确认对话框。

### Q2: 数据存储在哪里？

**A:** 系统采用双重存储策略：
- **后端数据库**：主数据存储，通过 API 访问
- **IndexedDB（本地）**：离线缓存，当后端不可用时自动降级为本地数据

### Q3: 支持离线使用吗？

**A:** 部分支持。系统使用 IndexedDB 作为本地缓存，当后端 API 不可用时会尝试从本地缓存读取数据。但创建、更新等写操作需要后端支持。

### Q4: 如何修改后端 API 地址？

**A:** 修改项目根目录下的 `.env.development` 文件中的 `VITE_API_BASE_URL` 变量，然后重启开发服务器。

### Q5: 筛选和排序配置会保存吗？

**A:** 会的。所有筛选、排序、分组、隐藏字段等视图配置都会随视图持久化保存，切换视图或刷新页面后配置不会丢失。

### Q6: 分组最多支持几级？

**A:** 最多支持 3 级分组。每级分组可以独立展开/折叠。

### Q7: 公式字段支持哪些函数？

**A:** 公式字段支持常用的数学函数、字符串函数、日期函数和逻辑函数。具体支持的函数列表可在公式编辑器中查看。

### Q8: 附件上传有什么限制？

**A:** 附件字段的限制可在字段配置中自定义，包括：
- 接受的文件类型（MIME 类型）
- 单个文件最大大小
- 总大小限制
- 最大/最小文件数量
- 是否生成缩略图

### Q9: 如何批量操作记录？

**A:** 在表格视图中，勾选多条记录后，右键可进行批量删除操作。后续版本将支持更多批量操作。

### Q10: 看板视图如何移动记录？

**A:** 在看板视图中，直接拖拽卡片到目标分组列即可移动记录，系统会自动更新分组字段的值。

### Q11: 如何自定义表单的外观？

**A:** 在表单视图的配置对话框中，可以自定义表单标题、描述、提交按钮文字、可见字段和提交成功提示。

### Q12: 仪表盘数据会实时更新吗？

**A:** 仪表盘支持配置自动刷新间隔。在仪表盘设置中可开启自动刷新并设置刷新间隔。

### Q13: 忘记密码怎么办？

**A:** 目前需要联系管理员通过后台管理页面重置密码。后续版本将增加密码找回功能。

### Q14: 实时协作功能如何启用？

**A:** 实时协作功能需要后端管理员启用。启用方式：
- 命令行启动时添加 `--enable-realtime` 参数：`python run.py --enable-realtime`
- 或设置环境变量：`ENABLE_REALTIME=true`
- Docker 部署时在 docker-compose.yml 中添加 `ENABLE_REALTIME=true`

如果后端未启用此功能，前端会自动隐藏协作相关界面，所有其他功能正常使用。

### Q15: 为什么看不到其他在线用户？

**A:** 可能的原因：
1. 后端未启用实时协作功能，请联系管理员启用
2. 网络连接中断，请检查页面底部的连接状态指示
3. 其他用户未打开同一表格

### Q16: 编辑冲突时如何处理？

**A:** 当系统检测到编辑冲突时，会弹出冲突解决对话框。您可以选择：
- **覆盖**：使用您的修改覆盖其他人的修改
- **刷新**：放弃您的修改，加载最新数据重新编辑

建议在多人协作时，先选中单元格查看是否有人正在编辑，避免冲突。

---

## 11. 注意事项

### 数据安全

1. **删除操作不可恢复**：删除多维表、数据表、字段或记录前请务必确认，删除后数据无法恢复
2. **定期备份**：建议定期通过导出功能备份重要数据
3. **分享链接安全**：创建分享链接时注意设置适当的权限，避免敏感数据泄露
4. **Token 安全**：系统使用 JWT Token 进行身份验证，Token 存储在本地，请勿在公共设备上保持登录

### 性能相关

1. **大数据量**：当单表记录数超过数千条时，建议使用筛选功能缩小数据范围
2. **分组性能**：3 级分组在大数据量场景下可能影响渲染性能
3. **附件大小**：大文件附件会影响加载速度，建议控制单个附件大小
4. **视图数量**：单个数据表建议不超过 10 个视图，过多视图会影响加载速度

### 浏览器兼容性

1. 推荐使用 Chrome、Edge、Firefox 等现代浏览器
2. 系统使用 IndexedDB，需要浏览器支持该特性
3. 部分功能（如文件拖拽上传）在旧版浏览器中可能不可用

### 使用建议

1. **合理规划字段**：创建数据表前先规划好字段类型和属性，避免后期频繁修改
2. **善用视图**：为不同场景创建不同视图，如一个用于日常编辑的表格视图，一个用于进度跟踪的看板视图
3. **利用模板**：新项目建议从模板创建，可以快速搭建数据结构
4. **设置默认值**：为常用字段设置默认值，提高数据录入效率
5. **使用关联字段**：跨表数据使用关联字段而非手动输入，保证数据一致性
6. **定期清理**：定期清理不需要的数据表和视图，保持工作空间整洁

### 开发相关

1. 项目使用 Vue 3 Composition API + TypeScript 开发
2. 状态管理使用 Pinia，数据流为：组件 → Store → Service → API/IndexedDB
3. 路由使用 Hash 模式（`createWebHashHistory`）
4. 数据库使用 Dexie.js 封装 IndexedDB，当前版本号为 5
5. API 请求使用 Axios，默认超时时间为 30 秒
6. 前端字段类型与后端字段类型存在映射关系（见 `normalizeFieldType` / `denormalizeFieldType`）

---

> 本手册最后更新于 2026-04-10，如有疑问或建议，欢迎提交 Issue 反馈。
